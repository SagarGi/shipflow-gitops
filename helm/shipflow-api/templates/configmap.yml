apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "shipflow-api.fullname" . }}-config
  labels:
    {{- include "shipflow-api.labels" . | nindent 4 }}

data:
{{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
{{- end }}